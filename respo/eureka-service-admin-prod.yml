spring:
  application:
    name: spring-cloud-service-admin

---
spring:
  profiles: peer1
server:
  port: 8772

eureka:
  instance:
    hostname: peer1
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

---
spring:
  profiles: peer2
server:
  port: 8773

eureka:
  instance:
    hostname: peer2
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/